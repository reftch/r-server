services:
  web-server:
    image: web-server:1.0.0
    build:
      context: ../..
      dockerfile: ./examples/docker/Dockerfile
    restart: unless-stopped
    environment:
      - PORT=8443
    ports:
      - 8443:8443
